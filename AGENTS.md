# game-prd-web 维护约定

本项目是独立、通用的 PRD 与交互原型网站，服务于网站、App 等产品。默认使用 Codex SDK，Claude SDK 是可选助手。

- 所有代码、默认 Skills、模板和测试须自包含，不读取相邻 Skill 仓库，不安装或修改其他项目。
- `server/domain.ts` 与 SQLite 是业务状态源；确认、豁免、正式知识采纳、删除和发布只能来自可信用户操作。
- AI 提交候选必须检查基础版本；历史版本与任务快照不可覆盖。扩展升级创建新 release，用户自定义扩展不自动升级。
- Codex/Claude 独立凭证、会话和工作目录；禁止扩展脚本执行、权限绕过与静默切换供应商。真实模型调用和测试 Mock 必须区分。
- `.data/`、`.env`、会话、日志、迁移备份与依赖不能进入 Git。数据迁移先停止写入、完整备份，再验证数量、关联与校验值；不得输出真实密钥。
- 修改前检查目标与调用方，保持黑白灰界面和现有功能，禁止重置未知改动。
- 变更能力同步 README、CHANGELOG 和相应 docs。默认 Skill 的契约随实现同步；不维护另一仓库的发布文档。
- 必跑 `npm test`、`npm run build`；交互变化追加 `npm run test:e2e`。隔离性变更还需在不含相邻仓库的干净目录验收。
- 未配置的真实服务标记待配置/待实测。不擅自提交、推送或部署。
