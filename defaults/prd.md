---
name: 研发执行版
description: 面向研发实现和验收的 PRD，可复制和自由调整标题与顺序。
---
# {{需求名称}}
文档信息：版本、状态、负责人、关联需求与原型版本、资料来源。

## 功能说明
用 1–3 句说明目标、范围、涉及模块。区分已上线事实与已确认目标。

## 总体流程
入口 → 资格判断 → 用户操作 → 结果。按需使用 Mermaid，不强制复杂图。

## 功能规则
| 规则 ID | 场景/条件 | 业务规则 | 客户端处理 | 服务端处理 | 最终结果 |
|---|---|---|---|---|---|
使用 R-001 等稳定 ID，不能把未确认结论写成事实。

## 页面与交互
<!-- condition: hasUI -->
关联具体原型版本、page ID、状态、动作和数据来源。无 UI 变化且已明确豁免时省略本章。

## 接口 / 数据 / 配置
<!-- condition: hasContractChanges -->
只写新增或改动的契约；明确字段类型、默认值、校验与所有权。

## 异常与边界
列出与本需求有关的失败、并发、重复提交、恢复与边界，不填泛泛清单。

## 验收标准
使用 AC-001 等编号，每条映射规则 ID；包含前置条件、动作和可观测结果。
