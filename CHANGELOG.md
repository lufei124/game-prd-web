# 变更记录

## 2026-09-08 · Conversation-first 工作台审查修复

- 修复首次发送需求从新建首屏进入工作区时 React effect 清理导致的白屏。
- 原型修改候选的应用与放弃改走任务级后端事务，持久化审计状态，避免刷新后候选复现或绕过候选直接创建版本。
- 知识库自动检索只向任务快照返回相关分块，不再附带整篇长文；补充中文、标题、文件名与正文命中回归覆盖。
- 评审建议只按每个问题的最新裁决筛选“采纳”，并保留来源角色；补齐 skipped 与 reviewed_then_modified 两条终稿血缘。
- Show Me 讲解版本改存 SQLite 并关联 PRD 版本；补齐旧版提示、HTML 导出和隔离 CSP。
- 新版 UI 增加成果历史回退与 stale PRD 重新同步，修复旧终稿存在时新 PRD head 无法再次确认。
- stale PRD 在上游原型尚未重新确认时禁止同步和确认终稿，并提示先完成原型确认。
- 当前有效终稿恢复飞书显式交付入口，仍先生成发布审批，再由用户二次确认写入远端。
- Playwright 用例切换为新版入口和 conversation-first 全流程，不恢复扩展中心、阶段 Skill 或 Claude UI。

## 2026-09-07 · 订阅与中央助手

- Codex 默认订阅认证，自动发现 CLI，支持官方设备登录、状态轮询与退出；API Key 需显式选择。
- 产品助手与输入框改为中间成果区下方，保留任务模型和思考深度设置。
- 回收站支持名称确认后永久删除与可恢复的清理重试。

## 未发布

- 修复 Codex Exec 继承未授权工作目录、输出 schema 位于沙箱外和系统 DNS 访问导致的启动失败。
- 移除对话任务设置面板，按阶段自动选择 Skill，支持对话明确指定；连续问答自动生成和更新需求卡。

- 精简设置页，保留配置状态与默认助手，折叠技术说明；Codex 已配置时阻止重复登录，退出后可重新登录。

- 知识库改为多级目录与文件，支持新增、移动、删除，旧模块迁为目录，未选择目录使用根目录。
- 移除知识提案入口；需求资料可引用知识库文件，对话支持 @ 选择并固定引用版本。

- 输入框改为白色主题，去掉“由你确认”提示，模型与思考深度改为就地弹出菜单。

- 需求工作区调整为资料 / 对话 / 成果三栏，输入框固定在中间栏底部，默认收起任务配置，避免对话被成果或输入框遮挡。

- 助手输入框采用参考图的圆角深灰布局，底部可直接选择任务模型与思考深度，附件及发送按钮保持真实功能。

- 将应用独立为通用 PRD 与交互原型网站，代码、默认资源和测试位于项目根目录，不依赖其他仓库。
- 提供通用中文需求整理、交互原型、低保真线框、PRD 编写和评审 Skills，保持原有风格与模板能力。
- 增加幂等默认资源升级，保留历史 release、成果、用户扩展和项目绑定，并移除旧源目录的自动授权。
- 保留 Codex 默认助手、模型/思考深度设置、知识与扩展中心、项目回收站、版本确认和飞书交付。
- 提供数据迁移核验脚本、独立文档与测试；真实模型和飞书能力仍需用户配置后实测。

### 产品助手对话

工作台只保留一个产品助手，默认通过 Codex SDK 自然聊天、逐轮澄清需求。普通问答不强制生成成果，需求澄清后自动将需求卡更新到右侧成果区；对话区显示交流与必要的思考、停止和重试状态，详细执行记录位于右侧“执行状态”。历史和版本保存在 SQLite，确认与发布仍需用户操作。保留现有独立凭证、工作目录及可选 Claude 配置，真实在线调用待实测。

助手输入框支持 Return 发送、Shift + Return 换行；输入法组字时不发送，@ 资料候选优先由 Return 选择。空消息或任务执行中不重复发送。

### 对话流程与阶段配置

- 移除聊天关键词猜测阶段和扩展的逻辑，区分当前阶段与显式生成操作；普通对话不强制产生版本。
- 需求确认进入原型，原型确认进入 PRD；默认允许通过对话修改原型交互与布局。
- 新增四阶段 Skill 配置、单选风格、独立 PRD 模板预览和低保真线框风格。
- 宿主启动三个独立模型会话进行评审，完整汇总后提交；PRD 修改后需重新评审才能定稿。
- 默认扩展 revision 2 升级前备份，新增 release，保留用户修改及历史版本。
